"""One-off and occasional commands, run by hand rather than by the scheduler.

Mostly preparation: building the reference data and baselines the running
system reads, importing a file someone was sent, or checking that a machine is
ready before a demo."""

