# Raw protocol documents

The documents as they were obtained, before being cut into passages.

Kept so that anything the system quotes can be traced back to the original, and
so the processing can be rerun without fetching everything again.

`classification.json` records what each document is about; `manifest.json`
records where each came from.
