# Local authority boundaries

The official jurisdiction boundaries, as published, in two file formats.

Used to build the web-ready boundary file the dashboard draws; nothing reads
these directly at run time.
