# Screen pieces

The map and everything drawn on or beside it: event cards, the detail window,
the legends, the layer switches, and the demo control.

`MapView.tsx` is the largest and owns the map itself; `layers/` holds the
individual things drawn on top of it.
