# Kinneret level advisory

Turns the published daily lake level into something an operator can read: which
band the lake is in, which way it is moving, and why that matters.

One file, `advisory.py`. No model, no prediction - the level is a fact and this
puts it in context.
