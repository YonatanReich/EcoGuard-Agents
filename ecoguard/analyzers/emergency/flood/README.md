# analyzers/emergency/flood/

Empty. Flood risk and inundation analysis lands here.

The protocol retriever (`ecoguard/shared/protocols.py`) is already
hazard-agnostic — `ProtocolRetriever(hazard="flood")` works the moment a
`flood/` corpus exists under `response_planner/protocols/`.
