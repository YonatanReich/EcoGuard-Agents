# Source material

The original fire service documents and the plan describing how they were
turned into something searchable.

Kept here for provenance. The processed passages the system actually reads live
under `ecoguard/data/protocols/`.
