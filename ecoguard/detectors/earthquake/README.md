# Earthquake detector

The simplest detector here. The Geological Survey publishes earthquakes that
have already been confirmed, so there is nothing to decide - a published
earthquake is an earthquake.

`observation_processing.py` turns the published records into the signal shape
the coordinator reads.
