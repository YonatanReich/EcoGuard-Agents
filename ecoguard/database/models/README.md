# Table definitions

The handful of tables described in code rather than only in a migration.

Most of the store is defined by the migrations next door; these exist because
something queries them through the ORM rather than with written SQL.
