# The migration steps

One file per change, numbered in the order they apply. Each one says what it
adds and how to undo it.

The numbering has a few merge points where two lines of work rejoined. Those
files add nothing themselves - they exist only to put the history back into a
single line.
